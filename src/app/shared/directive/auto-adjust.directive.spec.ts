import { AutoAdjustDirective } from './auto-adjust.directive';

describe('AutoAdjustDirective', () => {
  it('should create an instance', () => {
    // const directive = new AutoAdjustDirective();
    // expect(directive).toBeTruthy();
  });
});
